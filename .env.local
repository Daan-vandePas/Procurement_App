# Vercel KV Database Configuration
# These will be automatically set by Vercel in production
# For local development, create a Vercel KV database and add the values here

# KV_URL=your-kv-url-here
# KV_REST_API_URL=your-rest-api-url-here  
# KV_REST_API_TOKEN=your-rest-api-token-here
# KV_REST_API_READ_ONLY_TOKEN=your-read-only-token-here

# Note: When you deploy to Vercel, these will be auto-configured
# For now, we'll use a fallback storage mechanism